# Contributing

Contributions are welcome through issues and pull requests.

- keep capability names specific and documented;
- add fixtures and tests for new matcher behaviour;
- do not include real credentials, private addresses or identifiable infrastructure data;
- preserve deterministic output;
- explain interoperability effects and prior art for schema changes;
- run `python -m unittest discover -s tests -v` before submitting.
